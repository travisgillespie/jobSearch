var constants = {
  docId : worksheetID(),
  sheet1 : "Jobs",
}

var headers = ['title', 'url', 'state', 'city', 'company', 'jobkey', 'timestamp'];

var locationArray = ['Austin%2C+TX','San+Francisco%2C+CA','Remote'];

var jobSearch = {
  jobTitle: ['Project+Manager','Health+Program+Coordinator','Health+&+Human+Services','Health','Wellness'], //,'Health','Wellness'
  avoidJobs: ['assistant', 'associate','intern','part time'],
  salary: '' //90000
};
